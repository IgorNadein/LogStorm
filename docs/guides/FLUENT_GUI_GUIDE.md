# Fluent GUI

Fluent GUI сейчас заморожен. Исторические заметки о монолитном GUI больше не считаются актуальной документацией.

Актуальный статус:

- entrypoint: `run_gui.py`;
- код: `gui/`;
- статус: paused/experimental;
- обязательные проверки проекта: `pytest` для core/collector, без GUI smoke-import.

Если GUI будет возвращен в активную разработку, нужно сначала обновить зависимости, сценарии запуска и тестовый план.
